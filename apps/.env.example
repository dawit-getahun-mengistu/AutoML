POSTGRES_USER=postgres
POSTGRES_PASSWORD=123
POSTGRES_DB=userdbautoml

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=pgadmin-password

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=public"


ACCESS_TOKEN_SECRET = 'supersecretaccess' # change this to a more secure secret
REFRESH_TOKEN_SECRET= 'supersecretaccess' # change this to a more secure secret

RABBITMQ_HOST="rabbitmq"

DATA_PROFILING_REQUEST_QUEUE=DATA_PROFILING_REQUEST_QUEUE
DATA_PROFILING_RESULT_QUEUE=DATA_PROFILING_RESULT_QUEUE
